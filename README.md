# pizza-menu-2
A fresh Next.js pizza menu app that displays pizza options in the UI and serves menu data from a local API endpoint. 

## Commands
Create new app:
```bash
npx create-next-app@latest . --app
```